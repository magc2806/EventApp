<!-- app/views/shared/_navbar.html.erb -->
<nav class="fixed w-full top-0 left-0 z-10 bg-white shadow-md">
  <div class="container mx-auto px-6 py-4 flex justify-between items-center">
    <span class="text-2xl font-bold text-primary">EventManager</span>
    <div class="flex space-x-6">
      <%= link_to 'Home', root_path, class: 'text-neutral hover:text-primary'%>
      <% if user_signed_in? %>
        <%= link_to 'Events', events_path, class: 'text-neutral hover:text-primary' %>
        <%= link_to 'My profile', edit_user_registration_path, class: 'text-neutral hover:text-primary' %>
        <%= button_to "logout", destroy_user_session_path, method: :delete, class: 'text-neutral hover:text-primary'%>
      <%else%>
        <%= link_to 'Login', new_user_session_path, class: 'text-neutral hover:text-primary' %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'text-neutral hover:text-primary' %>
      <%end%>
    </div>
  </div>
</nav>
